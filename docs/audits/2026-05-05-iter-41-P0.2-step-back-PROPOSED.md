# Iter 41 P0.2 — Step-Back BASE_PROMPT v3.4 PROPOSED

**Data**: 2026-05-05 PM
**Status**: SHIPPED src edit local; **DEPLOY + R7 re-bench gated Andrea**
**Score impact ELAB**: projection R7 3.6% → **12-15%** (+8-12pt highest leverage R7 lift identified Sprint T close gate)

---

## §1 Atom shipped

**File**: `supabase/functions/_shared/system-prompt.ts` line 167-175 (NEW block 8 LOC inserted PRIMA "TAG INTENT CANONICO")
**Diff stat**: +8 -0
**Edit pattern**: hidden CoT pre-INTENT — model ragiona internamente categoria_astratta + componente_target + esperimentId lookup PRIMA di emettere [INTENT:{...}]. Output al docente resta ≤60 parole + INTENT tag (NO verbosity break, NO latency regression projected).

```
STEP-BACK INTERNO PRE-INTENT (iter 41 P0.2 — RAG-grounded reasoning, NO output al docente):
PRIMA di emettere [INTENT:{...}], ragiona internamente (catena di pensiero silenziosa, non scriverla):
1. categoria_astratta: una di [accendi-componente, evidenzia-componente, monta-esperimento, modifica-codice, mostra-help, naviga-volume, fai-quiz, salva-sessione, cattura-stato, controlla-circuito]
2. componente_target canonico: [led, resistore, breadboard, pin, pulsante, potenziometro, ldr, cicalino, motore-dc, lcd, servo, buzzer, fotoresistore, transistor, diodo, condensatore, capstone, ...] o null
3. esperimentId/azione canonical lookup dalla tua memoria 94 esperimenti (v1-cap6-esp1, v2-cap8-esp3, v3-cap8-serial, ecc.)
Solo DOPO la step-back interna emetti l'INTENT canonical conforme schema iter 38 ADR-030.
NON scrivere "categoria: ..." nell'output: l'output al docente resta breve ≤60 parole + INTENT tag.
```

---

## §2 Razionale tecnica

**Problema**: R7 canonical 3.6% iter 38 v54+v56 carryover (vs target ≥80%). L2 template router catches most prompts BEFORE Mistral function calling fires, AND when Mistral fires, the model emits non-canonical INTENT format (drift `experimentId` vs `id`, missing fields, wrong tool name).

**Hypothesis Step-Back** (Zhao et al 2024, applied prompt-engineering-guide §3 pick): forcing model to identify abstraction (categoria + componente) PRIMA di emettere structured output:
- Reduces hallucinated tool names (model anchored to canonical tool list)
- Reduces param drift (model explicit identifies componente_target → fills args correctly)
- Improves RAG grounding (canonical lookup esperimentId from 94 known)

Expected lift Step-Back-only: **R7 3.6% → 12-15%** (per Zhao et al benchmark range 10-36% lift, conservative ELAB-specific projection).

Stack with P1.1 Self-Consistency 3-vote + P1.2 ReAct → projection R7 18-22%, R5 95%+, R0 94%+.

---

## §3 Verification path (gated Andrea ratify)

**NOT autonomous executable**: requires Edge Function deploy + R7 bench + env keys.

```bash
# Step 1 — Andrea deploy
cd "/Users/andreamarro/VOLUME 3/PRODOTTO/elab-builder"
SUPABASE_ACCESS_TOKEN=sbp_XXX npx supabase functions deploy unlim-chat \
  --project-ref euqpdueopmlllqjmqnyb

# Step 2 — Smoke 5 prompts canonical INTENT
KEY=$(grep "^VITE_SUPABASE_EDGE_KEY" .env.production | cut -d= -f2-)
ELAB_KEY=$(grep "^ELAB_API_KEY" ~/.elab-credentials/sprint-s-tokens.env | cut -d= -f2-)
for prompt in "Carica esperimento LED" "Mostrami il LED" "Cattura screenshot" "Carica esperimento semaforo Vol3" "Evidenzia il resistore"; do
  curl -s -X POST https://euqpdueopmlllqjmqnyb.supabase.co/functions/v1/unlim-chat \
    -H "Authorization: Bearer $KEY" -H "apikey: $KEY" -H "X-Elab-Api-Key: $ELAB_KEY" \
    -H "Content-Type: application/json" \
    -d "{\"message\":\"$prompt\"}" | jq '.text, .intents_parsed'
done

# Step 3 — R7 200-prompt re-bench
SUPABASE_URL=https://euqpdueopmlllqjmqnyb.supabase.co \
  ELAB_API_KEY=$ELAB_KEY \
  VITE_SUPABASE_ANON_KEY=$KEY \
  node scripts/bench/run-sprint-r7-stress.mjs --fixture-path scripts/bench/r7-fixture.jsonl

# Step 4 — Validate canonical % delta vs iter 38 baseline 3.6%
# Expected ≥12% post-deploy. If <8%, REVERT (1 git checkout).
```

---

## §4 Anti-pattern G45 enforced

- ✅ NO claim "R7 lift LIVE" (deploy pending Andrea)
- ✅ NO claim "+8-12pt achieved" (projection only, NOT measured)
- ✅ NO `--no-verify` deploy
- ✅ NO push diretto main (working branch e2e-bypass-preview)
- ✅ Edit minimal 8 LOC + reversible (1 git checkout reverts)
- ✅ Hidden CoT design = NO ≤60 word cap break + NO latency regression projection
- ✅ Anti-inflation: PROPOSED status, NOT ACCEPTED — ratify gate Andrea Phase 4 deploy + R7 measurement

---

## §5 Dependency chain Sprint T close gate

P0.2 Step-Back depends NULL — autonomous src edit shipped iter 41.
P0.2 Step-Back **deploy** gated Andrea (env tokens + Phase 4 trigger).
P1.1 Self-Consistency requires P0.2 baseline R7 measured first.
P1.2 ReAct requires P1.1 baseline + Mistral tool wrapping.

Sprint T close 9.5/10 ONESTO realistic iter 43+ post:
- P0.2 R7 ≥12% verified
- P1.1+P1.2 stacked R7 ≥18%
- Andrea Path A primary (auth chain repair Vercel VITE_ELAB_API_KEY)
- 92→94 esperimenti audit Playwright (iter 21 carryover)
- Voyage re-ingest page metadata (R6 ≥0.55 unblock)
- Lighthouse perf 26+23 → ≥90
- Andrea Opus G45 indipendente review

---

## §6 Cross-link

- Roadmap parent: `docs/audits/2026-05-05-12-repo-roadmap-honest.md` §2.2 P0.2
- Iter 40 findings: `docs/audits/2026-05-05-iter-40-FINDINGS-honest.md`
- ADR-030 canonical schema: `docs/adrs/ADR-030-mistral-function-calling-intent-canonical.md`
- system-prompt.ts: `supabase/functions/_shared/system-prompt.ts:167-175`

End iter 41 P0.2 PROPOSED note. Andrea: ratify deploy + R7 re-bench when ready.
