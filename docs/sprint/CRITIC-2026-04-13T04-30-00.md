# Critic Adversariale — ELAB Tutor
> Data: 2026-04-13T04:30:00 | Metodo: Static analysis, source audit, git history, test run
> Niente complimenti. Solo problemi reali con severità e fix. Il prodotto va in vendita lunedì.

---

## EXECUTIVE SUMMARY

**Rispetto al critic delle 00:30 di stamattina**: 1 P0 UX fixato (UNLIM minimizzato), 1 P0 fixato (GF07 back browser), ma il critic precedente aveva sbagliato la diagnosi — vedi sotto. 0 security fix. CI gate ancora completamente rotto.

**Score onesto: 6.3/10** — +0.1 rispetto al critic delle 00:30. Modesto progresso.

**Stato test**: 3742 test (tutti PASS al run delle 04:02). Baseline CI ancora ferma a 1578.

---

## CORREZIONE AL CRITIC PRECEDENTE

### [IMPORTANTE] Il critic delle 00:30 aveva SBAGLIATO su UNLIM auto-expand

Il critic precedente dichiarava "P0 NON FIXATO — UNLIM auto-expand hack ancora presente". **È SBAGLIATO.**

**Situazione reale**: L'hack è ancora nel codice (`GalileoAdapter.jsx:380-388`) ma è **neutralizzato**:
- `LavagnaShell.jsx:337` → `useState(false)` per `galileoOpen`
- `LavagnaStateManager.js:26,32,38,44` → `galileo: false` in tutti gli stati non-STUCK
- L'hook ha guardia `if (!visible) return;` → se `galileoOpen=false`, l'hack non parte mai

Quando l'utente clicca la mascotte e UNLIM si apre, l'auto-expand fa espandere ChatOverlay — **comportamento desiderato**. Il P0 "UNLIM parte minimizzato" è **FIXATO** (commit a9461e7, Apr 12 21:47).

**Raccomandazione**: L'hack rimane codice brutto (querySelector + btn.click()). Va rimosso e sostituito con una prop `defaultExpanded` su ChatOverlay — ma non è un bug funzionale.

### [IMPORTANTE] GF07 back browser era GIÀ FIXATO

`App.jsx:126-130` ha `hashchange` + `popstate` handlers attivi dal commit C11. Il precedente critic lo aveva nella lista "must-fix" ma è stato fixato settimane fa.

---

## SICUREZZA — TUTTI E 3 INVARIATI

### [CRITICO] Supabase anon JWT hardcoded con fallback nel bundle
**File**: `src/services/api.js:19`
```js
const _SUPABASE_ANON = (import.meta.env.VITE_SUPABASE_EDGE_KEY || 'eyJhbGci...Q').trim();
```
Project ref `euqpdueopmlllqjmqnyb` sulla riga precedente. Bundle scaricabile da chiunque.
Segnalato il 12/04. Non fixato in 48h. 3 sessioni di sviluppo sono passate.
**Fix**: Rimuovere il fallback string. Se env manca, la build fallisce. 30 minuti.

### [ALTO] Admin hash SHA-256 senza salt nel bundle pubblico
**File**: `src/components/admin/AdminPage.jsx:78`
```js
const ADMIN_PWD_HASH = 'a5e8cdee037af3d0c6421f6bf766d840d38fed0c23ec56f24ffb56cc75616140';
```
Crackabile in secondi. Segnalato il 12/04. Non fixato.
**Fix breve**: Spostare auth admin su Supabase Auth. Non esiste fix cosmetic.

### [ALTO] Chiave di accesso `ELAB2026` in chiaro nel sorgente
**File**: `src/components/WelcomePage.jsx:107`
```js
if (trimmed.toUpperCase() !== 'ELAB2026') {
```
Chiunque vede il sorgente (F12 → Sources) accede al prodotto senza licenza. Nessuna revoca possibile.
**Fix**: Validazione via Supabase Edge Function. La chiave non deve mai comparire nel client.

---

## P0 UX — 1 ANCORA APERTO

### [CRITICO — NON FIXATO] Nessun "Prova gratis" sulla WelcomePage
**File**: `src/components/WelcomePage.jsx` (155 righe, solo un form con input + bottone ENTRA)

La route `#prova` ESISTE in `App.jsx:57,171` — è un simulatore funzionale Vol.1 senza login. Ma **WelcomePage.jsx non ha nessun link ad essa**.

Giovanni Fagherazzi arriverà alla homepage con un URL diretto. Non ha una chiave. Inserirà la prima cosa che gli viene in mente → "Chiave non valida" → abbandona.

La route `#prova` è una feature fantasma: implementata ma inaccessibile dalla UI principale.

**Fix**: 3 righe sotto il bottone ENTRA in WelcomePage.jsx:
```jsx
<div style={{ marginTop: 16, textAlign: 'center' }}>
  <button onClick={() => onNavigate?.('prova')} style={{ background: 'none', border: 'none', color: '#1E4D8C', fontSize: 14, cursor: 'pointer', textDecoration: 'underline' }}>
    → Prova senza registrarti (Vol.1 gratuito)
  </button>
</div>
```
Effort: 10 minuti. Impatto: se non fixato, il cliente principale non può entrare nel prodotto.

---

## CI GATE — ANCORA COMPLETAMENTE ROTTO (invariato)

### [CRITICO] E2E silenzioso — Playwright non blocca mai la CI
**File**: `.github/workflows/e2e.yml`
```yaml
run: npx playwright test || echo "No E2E tests configured yet"
```
Esistono **20 file E2E** con ~4660 righe di test. La CI li esegue e ignora i fallimenti. Non segnalato come fixato in nessun commit delle ultime 48h.
**Conseguenza**: puoi mergiare homepage rotta con CI verde.
**Fix**: Rimuovere `|| echo`. 2 minuti. I test E2E probabilmente non passano stabili — ma questo non giustifica silenziare il gate.

### [CRITICO] Baseline test count 2.4x sbagliata — protezione anti-regressione fantasma
**File**: `.test-count-baseline.json`
```json
{ "total": 1578, "date": "2026-04-09" }
```
Realtà al 13/04: **3742 test**. La baseline è 1578 → la soglia di allarme al 2% è `1578 * 0.98 = 1546`. Potresti rimuovere 2196 test prima che il ratchet scatti.
**Fix**: Aggiornare `total` a 3742. 1 minuto. Questo è inspiegabilmente rimandato da 4 giorni.

### [ALTO] Bundle limit 3500KB vs realtà 13MB — gate irrilevante
**File**: `.test-count-baseline.json` → `"bundle_max_kb": 3500`
**Realtà**: `dist/assets/*.js` = ~13MB totale. Il CI controlla il bundle ma il limite è 3.7x sotto la realtà.
Chunks maggiori (invariati):
- `index-BK1PKTFJ.js`: 2.1MB
- `react-pdf-Bc6Dw7aI.js`: 1.9MB
- `NewElabSimulator-DNepkP3i.js`: 1.3MB
- `ScratchEditor-CHrRbHZ1.js`: 732KB

**Fix breve termine**: Aggiornare `bundle_max_kb` al valore reale per eliminare falsi allarmi. Il bundle da 13MB rimane un problema di performance reale ma non si risolve in un giorno.

---

## QUALITÀ DEI NUOVI TEST — ANALISI CRITICA

Nelle ultime 24h sono stati aggiunti 131 test (3611 → 3742). Ma la qualità non è uniforme.

### [ALTO] AVR Bridge — 24 test utili MA avr8js completamente mockato
**File**: `tests/unit/avrBridge.test.js`
I test verificano pin mapping (PORTB/PORTD/PORTC), clamping ADC, parsing HEX Intel — **logica critica e correctamente testata**.
**Limite**: `vi.mock('avr8js', ...)` mocka l'intera CPU con stub. Il ciclo reale `HEX → CPU → pin output → CircuitSolver` non è mai eseguito.
Se avr8js cambia API breaking, i test rimangono verdi.
**Impatto**: medio — i pin mapping sono i test più critici e funzionano.

### [MEDIO] WCAG / Design System — 17+27 test che controllano CSS vars, non contrasto reale
**File**: `tests/unit/designSystem.test.js`
I test verificano che `--space-4` sia `16px`, che esista una media query `768`, che `--touch-min` sia `≥44px`.
**Non vengono testati**:
- Contrasto effettivo testo/sfondo (4.5:1 WCAG AA)
- Aria-label mancanti
- Focus trap nei dialog
- Screen reader compatibility

Il benchmark riportava "WCAG AA: 0 test" come debolezza critica. Ora ha 27 test ma questi testano il design token system, non la conformità WCAG reale.
**Impatto**: basso per la demo, ma dichiarare "WCAG AA: 10/10" nel benchmark sarebbe disonesto.

### [BASSO] UNLIM test — 63 test su regex, non sul flusso AI reale (invariato dal critic precedente)
Segnalato il 13/04T00:30. Non fixato. Il cuore del prodotto (UNLIM risponde e il messaggio appare) rimane non testato con dati reali.

---

## NUOVI PROBLEMI TROVATI OGGI

### [MEDIO] Route `#prova` non gestita nel menu di navigazione mobile
**File**: `App.jsx:57` → `VALID_HASHES` include `prova`
Quando un utente in modalità `prova` clicca il bottone indietro, torna alla showcase (`getPageFromHash() || 'showcase'`). Ma se naviga direttamente a `/#prova` da mobile senza hash precedente, e clicca "Esci" o equivalente — dove va? Verificare che l'UX di uscita dalla modalità prova sia gestita.

### [MEDIO] FatturaElettronicaService — TODO silenzioso nel codice fiscale
**File**: `src/components/admin/gestionale/services/FatturaElettronicaService.js:11`
`METODO_PAGAMENTO_MAP` è definito ma la riga 246 usa un fallback `|| 'MP05'` (contanti) per qualunque metodo sconosciuto. Se viene passato un metodo non mappato, la fattura elettronica XML avrà sempre `MP05` silenziosamente. Rilevante se il gestionale viene usato per fatture reali.
**Impatto**: alto per integrità fatture, basso per la demo.

### [BASSO] `VetrinaSimulatore.jsx` importata ma non usata — possibile dead import
`App.jsx` non importa `VetrinaSimulatore` (usa `VetrinaV2`). Ma il file `VetrinaSimulatore.jsx` esiste ancora in `src/components/`. Vite non lo include nel bundle se non importato, ma è source code morto che crea confusione.

---

## CONFRONTO CON CRITIC PRECEDENTE (13/04T00:30)

| Issue | 00:30 | 04:30 | Note |
|-------|-------|-------|------|
| UNLIM auto-expand | CRITICO | **FIXATO** | Il critic 00:30 era sbagliato |
| GF07 back browser | CRITICO | **FIXATO** | Era già fixato da prima |
| Supabase JWT | CRITICO | **CRITICO** | Non fixato (48h) |
| Admin hash | ALTO | **ALTO** | Non fixato (48h) |
| WelcomePage key | ALTO | **ALTO** | Non fixato (48h) |
| No "Prova gratis" | CRITICO | **CRITICO** | Non fixato |
| E2E silenzioso | CRITICO | **CRITICO** | Non fixato |
| Baseline 1578 | ALTO | **ALTO** | Non fixato (4 giorni) |
| Bundle gate 3500KB | ALTO | **ALTO** | Non fixato |
| UNLIM test su regex | ALTO | **ALTO** | Non fixato |
| Route #prova non collegata | — | **MEDIO NUOVO** | Trovato oggi |
| FatturaElettronicaService fallback | — | **MEDIO NUOVO** | Trovato oggi |

---

## PRIORITÀ ASSOLUTE PER SESSIONE 13/04

### OBBLIGATORIO — fa la differenza lunedì (45 minuti totali)

1. **Collegare `#prova` da WelcomePage** — 10 min. Senza questo: Giovanni non entra.
2. **Fix CI baseline** (`total: 3742`, `bundle_max_kb: 13000`) — 2 min. Senza questo: ratchet fantasma.
3. **Fix E2E silenzioso** (rimuovere `|| echo`, far girare almeno 01-homepage stable) — 30 min.

### IMPORTANTE — post vendita

4. Rimuovere JWT fallback da `api.js`
5. Admin auth → Supabase Auth
6. WelcomePage key → server-side validation
7. UNLIM integration tests reali (renderHook + fetch mock che ritorna risposta valida)
8. WCAG reale con axe-playwright, non solo CSS vars
9. Bundle lazy-load (react-pdf, mammoth) — 1.9MB+499KB liberabili

---

## PATTERN PERICOLOSI (AGGIORNATI)

| Pattern | Count | Status |
|---------|-------|--------|
| `console.log` (non-logger) | 1 | Accettabile (codeProtection.js, arte ASCII) |
| `TODO` | 2 | Basso rischio (FatturaElettronica, non core) |
| `HACK` | 0 | OK |
| Google API key (`AIza`) | 0 | OK |
| JWT hardcoded | 1 | **CRITICO** — non fixato |
| Password hash in bundle | 1 | **ALTO** — non fixato |
| Access key in plain text | 1 | **ALTO** — non fixato |
