# Critic Adversariale — ELAB Tutor
> Data: 2026-04-13T06:57:33 | Metodo: Static analysis, grep, git log, test run, bundle audit
> Niente complimenti. Solo problemi reali con severità e fix. Il prodotto va in vendita lunedì.

---

## EXECUTIVE SUMMARY

**Score onesto: 6.3/10** — identico al critic delle 04:30. Zero progress nelle ultime 2.5 ore.

**Cosa è successo dall'ultimo critic (04:30):**
- 2 commit di test (AVR + Design System + WCAG AA): **utili ma non urgenti**
- 4 commit di documentazione/planning: **zero impatto sul prodotto**
- **0 security fix** — i 3 problemi critici di sicurezza sono invariati
- **0 P0 UX fix** — "Prova gratis" ancora mancante dalla homepage
- **0 fix CI** — baseline ancora 1578, E2E ancora silenzioso

Il team sta scrivendo documentazione e test su categorie già discrete invece di fixare i problemi che bloccano la vendita. Priorità invertite.

---

## SICUREZZA — TUTTI E 3 INVARIATI (72h+)

### [CRITICO — 72h INVARIATO] JWT Supabase hardcoded nel bundle
**File**: `src/services/api.js:19`
```js
const _SUPABASE_ANON = (import.meta.env.VITE_SUPABASE_EDGE_KEY || 'eyJhbGci...Q').trim();
```
Il fallback JWT è nel bundle JS scaricabile da chiunque in chiaro. Questo esiste da **almeno 72 ore** segnalato nei critic — non è stato toccato in 6 sessioni di sviluppo.

**Fix**: Rimuovere il fallback. Se env manca, la build fallisce esplicitamente. 30 minuti.

### [ALTO — 72h INVARIATO] Admin hash SHA-256 senza salt nel bundle
**File**: `src/components/admin/AdminPage.jsx:78`
```js
const ADMIN_PWD_HASH = 'a5e8cdee037af3d0c6421f6bf766d840d38fed0c23ec56f24ffb56cc75616140';
```
Ogni rainbow table lo crackabile in secondi. La password admin è reversibile.
**Fix reale**: Supabase Auth per l'admin. Non esiste patch cosmética.

### [ALTO — 72h INVARIATO] Chiave `ELAB2026` in chiaro nel sorgente
**File**: `src/components/WelcomePage.jsx:107`
```js
if (trimmed.toUpperCase() !== 'ELAB2026') {
```
F12 → Sources → cerca "ELAB" → accesso gratuito a vita, senza possibilità di revoca.
**Fix**: Validazione via Supabase Edge Function.

---

## P0 UX — 1 ANCORA APERTO (invariato dal 12/04)

### [CRITICO — NON FIXATO] "Prova gratis" assente dalla WelcomePage

`src/components/WelcomePage.jsx` — 155 righe, zero menzione di "prova", "demo", "gratis".

La route `#prova` esiste in `App.jsx:57,171` — è un simulatore funzionale Vol.1. Ma è **inaccessibile dalla UI**. Feature implementata e nascosta.

Giovanni Fagherazzi arriverà con un URL. Non ha una chiave. Arriverà a `/#` → inserirà qualcosa → "Chiave non valida" → chiuderà la tab. Il cliente principale non può entrare nel prodotto.

**Fix** (10 minuti):
```jsx
// src/components/WelcomePage.jsx — dopo il bottone ENTRA
<div style={{ marginTop: 16, textAlign: 'center' }}>
  <button onClick={() => onNavigate?.('prova')}
    style={{ background: 'none', border: 'none', color: '#1E4D8C',
             fontSize: 14, cursor: 'pointer', textDecoration: 'underline' }}>
    → Prova senza registrarti (Vol.1 gratuito)
  </button>
</div>
```

---

## CI GATE — INVARIATO DA GIORNI

### [CRITICO] E2E silenzioso — Playwright non blocca mai la CI
**File**: `.github/workflows/e2e.yml`
```yaml
run: npx playwright test || echo "No E2E tests configured yet"
```
Esistono **20 file E2E** in `e2e/` (inclusi stress test da 470+ righe per 50 utenti concorrenti). Il CI li esegue e ingoia i fallimenti. Puoi mergiare homepage rotta con CI verde.

Ironia: i test E2E ci sono, funzionano, ma la CI è configurata per ignorarli. La protezione esiste ma è disabilitata.

**Fix**: Rimuovere `|| echo`. Poi verificare che almeno `01-homepage-simulator.spec.js` passi stable — 30 min.

### [CRITICO] Baseline test count sbagliata di 2.4x — protezione anti-regressione fantasma
**File**: `.test-count-baseline.json`
```json
{ "total": 1578, "date": "2026-04-09", "bundle_max_kb": 3500 }
```
**Realtà 13/04 ore 06:57**: 3742 test, bundle 15MB.

- Soglia anti-regressione al 2% = `1578 * 0.98 = 1546`. Potresti rimuovere **2196 test** (59% del totale!) prima che il ratchet scatti.
- `bundle_max_kb: 3500` vs realtà 15000KB — il gate è falso.

**Fix**: 2 file → 1 minuto. Questo non è stato aggiornato in **4 giorni**.

---

## NUOVO: 103 BRANCH AUTO NON MERGIATI — PROBLEMA CRITICO

**Trovato adesso** — non era nei critic precedenti.

`git branch -a --no-merged main | grep "auto/"` → **103 branch non mergiati in main**.

Tra questi, fix già scritti e testati che **migliorano la sicurezza e la qualità**:

| Branch | Fix | Stato |
|--------|-----|-------|
| `auto/bugfix-gdpr-email-0740` | Hash parentEmail prima di localStorage (PII compliance) | NON MERGIATO |
| `auto/fix-fetch-timeout` | AbortSignal.timeout su 2 fetch non protetti | NON MERGIATO |
| `auto/20260406-wcag-safety-setup` | WCAG critical — aria-label, aria-invalid | NON MERGIATO |
| `auto/test-api` | +10 test api.js | NON MERGIATO |
| `auto/test-auth-0253` | +20 test authService | NON MERGIATO |

Il branch `auto/bugfix-gdpr-email-0740` in particolare risolve un problema di **PII compliance** (parentEmail hashato prima di salvarlo in localStorage) che è rilevante con minori. È pronto, testato, e non è mai stato mergiato.

**Cosa fare**: Non mergiare ciecamente 103 branch. Ma `auto/bugfix-gdpr-email-0740` e `auto/fix-fetch-timeout` vanno revisionati e mergiati oggi se passano i test.

---

## QUALITÀ TEST — ANALISI AGGIORNATA

### [MEDIO] WCAG test esistono ma non misurano conformità reale
**File**: `tests/unit/designSystem.test.js` (27 test aggiunti oggi)

I test verificano che `--space-4 === 16px`, che `--touch-min >= 44px`, che esista una media query 768. Corretto ma **non è WCAG AA**.

WCAG AA richiede:
- Contrasto 4.5:1 testo/sfondo (non testato — richiederebbe axe-playwright)
- aria-label su tutti i controlli interattivi (non testato in unit test)
- Focus visible su tutti gli elementi interattivi (non testato)

Dichiarare "WCAG AA: 10/10" nel benchmark su questi test sarebbe disonesto.

### [MEDIO] UNLIM test — ancora su regex, non su flusso AI reale
**File**: `tests/unit/unlimChat.test.js` (32 test aggiunti il 12/04)

`global.fetch = vi.fn(() => Promise.resolve({ ok: false, ... }))` — ogni test simula un fetch fallito. Si testa la moderation layer e il rate limiter, ma **mai** il percorso felice: UNLIM riceve domanda → fetch → risposta AI → appare nel chat UI.

Il benchmark ora segna "UNLIM Chat: 10/10". È inflato — i test coprono le guardrail, non il core.

### [BASSO] AVR Bridge — pin mapping testato, ciclo CPU no
Invariato dal critic precedente. I 24 test di AVRBridge sono utili per il pin mapping (regola critica CLAUDE.md: D0-D7=PORTD, ecc.), ma `avr8js` è completamente mockato. Il flusso reale `HEX → CPU → GPIO → CircuitSolver` non è mai eseguito.

---

## PATTERN PERICOLOSI — AGGIORNAMENTO

| Pattern | Count | Status | Note |
|---------|-------|--------|------|
| `console.log` (non-logger) | 4 | ✅ Accettabile | logger.js (2, condizionali), codeProtection.js (ASCII art), simulator-api.js (JSDoc comment — non eseguito) |
| `TODO` | 2 | ⚠️ Basso rischio | FatturaElettronicaService — non core per la demo |
| `HACK` | 0 | ✅ OK | — |
| Google API key (`AIza`) | 0 | ✅ OK | Nessuna key Google nel bundle |
| JWT hardcoded | 1 | 🔴 CRITICO | api.js:19 — invariato 72h |
| Password hash in bundle | 1 | 🟠 ALTO | AdminPage.jsx:78 — invariato 72h |
| Access key plaintext | 1 | 🟠 ALTO | WelcomePage.jsx:107 — invariato 72h |

---

## CONFRONTO CON CRITIC PRECEDENTE (04:30)

| Issue | 04:30 | 06:57 | Delta |
|-------|-------|-------|-------|
| JWT Supabase hardcoded | CRITICO | **CRITICO** | Invariato |
| Admin hash | ALTO | **ALTO** | Invariato |
| WelcomePage key | ALTO | **ALTO** | Invariato |
| No "Prova gratis" | CRITICO | **CRITICO** | Invariato |
| E2E silenzioso | CRITICO | **CRITICO** | Invariato |
| Baseline 1578 | ALTO | **ALTO** | Invariato (4 giorni) |
| Bundle gate 3500KB | ALTO | **ALTO** | Invariato |
| 103 branch non mergiati | — | **NUOVO ALTO** | Trovato oggi |
| UNLIM test regex-only | ALTO | **ALTO** | Invariato |
| WCAG test non reali | MEDIO | **MEDIO** | 27 token test aggiunti ma non WCAG reale |

---

## PRIORITÀ ASSOLUTE PER SESSIONE 13/04 (lunedì mattina)

Il prodotto va in vendita. Ecco cosa fa la differenza:

### OBBLIGATORIO — impatto diretto sulla demo (1 ora totale)

1. **Collegare `#prova` da WelcomePage** — 10 min. Senza questo Giovanni non entra.
2. **Fix baseline** (`total: 3742`, `bundle_max_kb: 15000`) — 2 min. Senza questo il ratchet è inutile.
3. **Fix E2E gate** (rimuovere `|| echo`, verificare 01-homepage stable) — 30 min.

### IMPORTANTE — sicurezza pre-lancio (2-4 ore)

4. Review + merge `auto/bugfix-gdpr-email-0740` — PII compliance con minori.
5. Review + merge `auto/fix-fetch-timeout` — fetch non protetti (AbortSignal).
6. Rimuovere JWT fallback da `api.js` — 30 min.

### POST VENDITA — non blocca lunedì

7. Admin auth → Supabase Auth
8. WelcomePage key → server-side validation
9. UNLIM integration test reale (fetch mock che ritorna risposta valida)
10. WCAG reale con axe-playwright
11. Bundle lazy-load (react-pdf 1.8MB + mammoth 487KB liberabili con `import()`)

---

## NOTE FINALI

Il prodotto ha 3742 test che passano tutti. La struttura è solida. Ma le sessioni degli ultimi 2 giorni hanno prodotto principalmente **documentazione, planning, e test su categorie già buone** invece di chiudere i 3 fix bloccanti che esistono da 72 ore.

I 103 branch auto non mergiati sono il simbolo del problema: il sistema autonomo produce fix validi, ma senza review e merge sistematici diventano debito nascosto.

Lunedì mattina, se Giovanni arriva alla homepage e non riesce ad entrare perché non ha una chiave, nessuna documentazione o test aggiuntivo lo salva.
